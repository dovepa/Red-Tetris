{
  "name": "redtetrisapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install": "cd server && npm i  && npm run build && cd ../client && npm i  && npm run build",
    "build": "cd server && npm run build && cd ../client && npm run build",
    "start": "node server/dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@angular-devkit/build-angular": "^0.901.7",
    "@angular/cli": "^9.1.7",
    "@angular/compiler-cli": "^9.1.9",
    "@types/node": "^14.0.11"
  }
}
